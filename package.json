{
  "name": "tk-cliptest",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",
    "build": "electron-builder --win --x64 --dir"
  },
  "build": {
    "files": [
      "!fallback"
    ],
    "extraFiles": [
      "fallback"
    ]
  },
  "author": "lpre_ys",
  "license": "MIT",
  "devDependencies": {
    "electron": "^21.0.1",
    "electron-builder": "^23.3.3"
  },
  "dependencies": {
    "iconv-lite": "^0.6.3"
  }
}
